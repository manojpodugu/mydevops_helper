Consider the following folder structure in Windows. We will push "folder2" along with "file1" to GITHUB REPO using GIT_BASH.
------------------------------------------------------------------------------------------------------------------------------------
folder1/
├── folder2/
└── file1.txt
--------------------------------------------------------------------------------------------------------------------------------------





git remote-v

No (no Output) NO

ed disfolder

→file



git git add. 10

in the pwd to sta git in't to push folder-1. staging avee

→ To add all the git status to see all the files staged changes

giturm-cached To remove all staged changes

git commit -m "meg" To Commit the staged changed

git, update-of-d HEAD→ To un commit the changes &

Send them back to "staged Changes"

NOTE:- Git Cannot push the to cal files repo. To do. So, to an
